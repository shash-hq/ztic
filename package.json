{
  "name": "ztic-root",
  "version": "1.0.0",
  "description": "ZTic monorepo root — run client + server concurrently",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" --names \"SERVER,CLIENT\" --prefix-colors \"red,cyan\"",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
